# Frontend

The frontend is a React + Vite client for the Spotter route-planning assessment. Full project documentation, local setup, API details, assumptions, testing commands, and deployment notes are in the repository root [README](../README.md).

```bash
npm install
npm run dev
```
